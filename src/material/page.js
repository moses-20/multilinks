import { styled } from "@mui/material";

const Page = styled("div")({
  width: "100%",
  display: "flex",
  minHeight: "100vh",
  overflow: "hidden",
  flexDirection: "column",
  backgroundColor: "whitesmoke",
});

export default Page;
